[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-core"
version = "0.1.0"
description = "Shared library for CloudAura RAG Patterns Showcase"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["rag_core*"]
